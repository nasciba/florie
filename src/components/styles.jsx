import styled from 'styled-components';

const StyledDisplay = styled.div`
    display: flex;
    flex-wrap: wrap;
    justify-content: center; 
    margin: 50px;
    
    
`

export {
    StyledDisplay,
}